import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.efecode.appprueba',
  appName: 'appPrueba',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
